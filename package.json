{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.2.0"
  },
  "scripts": {
    "start": "npx nodemon index"
  },
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "nodemonConfig": {
    "ignore": [
      ".git",
      "node_modules/**/*",
      "README.md",
      "public/*"
    ]
  }
}
